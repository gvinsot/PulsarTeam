version: '3.8'
services:
  api:
    image: registry.methodinfo.fr/agentswarm-server:latest
    build:
      context: ..
      dockerfile: api/Dockerfile
      target: production
    environment:
      - NODE_ENV=production
      - PORT=${PORT}
      - JWT_SECRET=${JWT_SECRET}
      - ADMIN_USERNAME=${ADMIN_USERNAME}
      - ADMIN_PASSWORD=${ADMIN_PASSWORD}
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - OLLAMA_BASE_URL=${OLLAMA_BASE_URL}
      - MISTRAL_API_KEY=${MISTRAL_API_KEY}
      - CORS_ORIGINS=${CORS_ORIGINS}
      - DATABASE_URL=${DATABASE_URL}
      - GIT_USER_NAME=${GIT_USER_NAME}
      - GIT_USER_EMAIL=${GIT_USER_EMAIL}
      - SANDBOX_SHARED_CONTAINER_NAME=sandbox
      - SANDBOX_BASE_WORKSPACE=/workspace
      - SSH_KEYS_HOST_PATH=${SSH_KEYS_HOST_PATH}
      - GITHUB_TOKEN=${GITHUB_TOKEN}
      - GITHUB_USER=${GITHUB_USER}
      - MCP_ENDPOINT=http://swarm-manager:8000/ai/mcp
      - ONEDRIVE_CLIENT_ID=${ONEDRIVE_CLIENT_ID}
      - ONEDRIVE_CLIENT_SECRET=${ONEDRIVE_CLIENT_SECRET}
      - ONEDRIVE_REDIRECT_URI=${ONEDRIVE_REDIRECT_URI}
      - ONEDRIVE_TENANT_ID=${ONEDRIVE_TENANT_ID}
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock  # Docker CLI for sandbox management
      - ${SSH_KEYS_HOST_PATH:-/tmp/empty-ssh}:/root/.ssh:ro  # SSH keys (also mounted into sandboxes)
    networks:
      - proxy
      - postgresqlcluster_internal
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3001/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    deploy:
      replicas: 1
      placement:
        constraints:
          - node.hostname == server-b
      restart_policy:
        condition: on-failure
      labels:
        # Enable Traefik
        - "traefik.enable=true"

        # Service port
        - "traefik.http.services.agentswarm-api.loadbalancer.server.port=3001"

        # HTTPS Router — API + WebSocket (/api and /socket.io)
        # Using realtime-global (no WAF) to avoid blocking AI chat content with code patterns
        - "traefik.http.routers.agentswarm-api.rule=Host(`swarm.methodinfo.fr`) && (PathPrefix(`/api`) || PathPrefix(`/socket.io`))"
        - "traefik.http.routers.agentswarm-api.entrypoints=websecure"
        - "traefik.http.routers.agentswarm-api.tls.certresolver=letsencrypt"
        - "traefik.http.routers.agentswarm-api.middlewares=realtime-global@file"
        - "traefik.http.routers.agentswarm-api.service=agentswarm-api"
        - "traefik.http.routers.agentswarm-api.priority=100"

        # HTTP redirect
        - "traefik.http.routers.agentswarm-api-http.rule=Host(`swarm.methodinfo.fr`) && (PathPrefix(`/api`) || PathPrefix(`/socket.io`))"
        - "traefik.http.routers.agentswarm-api-http.entrypoints=web"
        - "traefik.http.routers.agentswarm-api-http.middlewares=redirect-to-https@file"
        - "traefik.http.routers.agentswarm-api-http.service=noop@internal"

  frontend:
    image: registry.methodinfo.fr/agentswarm-client:latest
    build:
      context: ..
      dockerfile: frontend/Dockerfile
    volumes:
      - ${HOST_CODE_PATH}/AgentsSwarmUI/frontend/dist:/usr/share/nginx/html
    networks:
      - proxy
    deploy:
      replicas: 1
      placement:
        constraints:
          - node.hostname == server-b
      restart_policy:
        condition: on-failure
      labels:
        # Enable Traefik
        - "traefik.enable=true"

        # Service port
        - "traefik.http.services.agentswarm-frontend.loadbalancer.server.port=80"

        # HTTPS Router — Frontend (catch-all for swarm.methodinfo.fr)
        - "traefik.http.routers.agentswarm-frontend.rule=Host(`swarm.methodinfo.fr`)"
        - "traefik.http.routers.agentswarm-frontend.entrypoints=websecure"
        - "traefik.http.routers.agentswarm-frontend.tls.certresolver=letsencrypt"
        - "traefik.http.routers.agentswarm-frontend.middlewares=global@file"
        - "traefik.http.routers.agentswarm-frontend.service=agentswarm-frontend"
        - "traefik.http.routers.agentswarm-frontend.priority=1"

        # HTTP redirect
        - "traefik.http.routers.agentswarm-frontend-http.rule=Host(`swarm.methodinfo.fr`)"
        - "traefik.http.routers.agentswarm-frontend-http.entrypoints=web"
        - "traefik.http.routers.agentswarm-frontend-http.middlewares=redirect-to-https@file"
        - "traefik.http.routers.agentswarm-frontend-http.service=noop@internal"

  coder-service:
    build:
      context: ..
      dockerfile: coder-service/Dockerfile
    image: registry.methodinfo.fr/coder-service:latest
    networks:
      proxy:
        aliases:
          - coder-service
    environment:
      - TZ=Europe/Paris
      - LOG_LEVEL=info
      # Claude Code authentication (OAuth token uses subscription plan, API key uses credits)
      - CLAUDE_CODE_OAUTH_TOKEN=${CLAUDE_CODE_OAUTH_TOKEN:-}
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-}
      - CLAUDE_MODEL=${CLAUDE_MODEL:-claude-opus-4-6}
      - CLAUDE_MAX_TURNS=${CLAUDE_MAX_TURNS:-50}
      # Execution settings
      - VERBOSE=${CODER_VERBOSE:-false}
      - TIMEOUT=600
      - PORT=8000
      - HOST=0.0.0.0
      # Security - API key authentication
      - API_KEY=${CODER_API_KEY:-change-me-in-production}
      # Run as host user (match /projects owner for file permissions)
      - PUID=${CODER_PUID:-1000}
      - PGID=${CODER_PGID:-1000}
      # Python settings
      - PYTHONUNBUFFERED=1
      - PYTHONFAULTHANDLER=1
    volumes:
      # Mount project directories
      - /home/gildas:/projects
      # SSH keys for Git access (read-only)
      - /home/gildas/.ssh:/root/.ssh:ro
      # Docker socket for Docker-in-Docker
      - /var/run/docker.sock:/var/run/docker.sock
      # Persistent auth data (OAuth tokens survive container restarts)
      - coder-auth-data:/app/data
      # JDK setup for Android development
      - coder-android-cache:/opt/android-sdk
    deploy:
      replicas: 1
      placement:
        constraints:
          - node.hostname == server-b
      resources:
        limits:
          cpus: '4'
          memory: 8G
        reservations:
          cpus: '2'
          memory: 4G
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 3
      update_config:
        delay: 10s
        failure_action: rollback
      labels:
        # Activer Traefik
        - "traefik.enable=true"

        # Définir le port du service Coder
        - "traefik.http.services.coder-service.loadbalancer.server.port=8000"
        # Flush immédiat pour le streaming SSE
        - "traefik.http.services.coder-service.loadbalancer.responseforwarding.flushinterval=1ms"

        # Router HTTPS (principal) - Accès public
        - "traefik.http.routers.coder-service.rule=Host(`${CODER_DOMAIN:-coder.methodinfo.fr}`)"
        - "traefik.http.routers.coder-service.entrypoints=websecure"
        - "traefik.http.routers.coder-service.tls.certresolver=letsencrypt"
        - "traefik.http.routers.coder-service.service=coder-service"

        # Router HTTP (redirige vers HTTPS)
        - "traefik.http.routers.coder-service-http.rule=Host(`${CODER_DOMAIN:-coder.methodinfo.fr}`)"
        - "traefik.http.routers.coder-service-http.entrypoints=web"
        - "traefik.http.routers.coder-service-http.middlewares=redirect-to-https@file"
        - "traefik.http.routers.coder-service-http.service=noop@internal"

    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 120s

  test:
    image: registry.methodinfo.fr/agentswarm-test:latest
    build:
      context: ..
      dockerfile: api/Dockerfile
      target: test
    deploy:
      replicas: 0  # Only used for CI — never deployed as a service

  sandbox:
    image: ${DOCKER_REGISTRY_URL}/agentswarm-sandbox:latest
    build:
      context: ..
      dockerfile: api/sandbox.Dockerfile
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ${SSH_KEYS_HOST_PATH:-/tmp/empty-ssh}:/root/.ssh:ro
    networks:
      - proxy
    deploy:
      replicas: 1
      placement:
        constraints:
          - node.hostname == server-b
      restart_policy:
        condition: on-failure

volumes:
  coder-android-cache:
    driver: local
  coder-auth-data:
    driver: local

networks:
  proxy:
    external: true
  postgresqlcluster_internal:
    external: true